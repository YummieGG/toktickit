PORT=3000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/toktickit_db?schema=public"
APP_ORIGIN="http://localhost:5173"
AUTH_SECRET="replace-with-a-local-secret"
AUTH_IP_HMAC_SECRET="replace-with-a-separate-local-secret"
# Set locally to a password that satisfies the 12–128 character policy.
# Never commit the real value.
SEED_INITIAL_PASSWORD="<set-locally-do-not-commit>"
